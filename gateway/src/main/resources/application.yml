server:
  port: 8000

spring:
  application:
    name: gateway
  jackson:
    default-property-inclusion: non_null
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
#          default-filters:
#            - RewritePath=/api/v1/(?<segment>.*), /${segment}
#          filter:
#            rewrite-location:
#              enabled: true