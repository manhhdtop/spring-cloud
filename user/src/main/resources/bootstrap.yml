eureka:
  client:
    service-url:
      defaultZone: ${EUREKA.URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true
    ip-address: ${EUREKA.IP:127.0.0.1}

spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: default,redis,rabbitmq
